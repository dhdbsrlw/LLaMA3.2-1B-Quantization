seed: 42
device: cuda
output_dir: ./result/analyze

model_path: /nas2/checkpoints/Llama-3.2-1B
tokenizer_path: 

model_type: pretrain

# store_true
use_bfloat: False

ckpt:
lora_ckpt:

max_seq_len: 128
num_calib_data: 10
num_pruned_blocks: 1 # fixed
